; NPM Security Configuration
; Prevent arbitrary command execution during install
ignore-scripts=true

; Use exact versions for security-critical dependencies
save-exact=false

; Enable strict peer dependency checking
legacy-peer-deps=false

; Use latest npm audit for security vulnerability detection
; Run: npm audit before deployment

; Always verify package signatures when possible
prefer-online=true

; Use the latest npm version
update-notifier=true
